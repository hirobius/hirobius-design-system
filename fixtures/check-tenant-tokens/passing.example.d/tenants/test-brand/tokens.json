{
  "$schema": "../../hirobius.tenant-tokens.schema.json",
  "$description": "Passing fixture: valid semantic-tier override against a known base path.",
  "semantic": {
    "color": {
      "$type": "color",
      "surface": {
        "accent": {
          "$value": "{primitive.color.blue.500}",
          "$description": "Tenant accent override — valid alias, valid path."
        }
      }
    }
  }
}
