/**
 * MobiusConstants — lightweight constants shared between HDSLayout and
 * MobiusShellLayer without pulling in the three.js dependency graph.
 *
 * @internal — not part of @hirobius/design-system public API surface.
 * @tier utility
 */

/** CSS attribute selector that identifies the shell-level nav Möbius anchor. */
export const NAV_MOBIUS_ANCHOR = '[data-mobius-anchor="shell-top-nav-mobius"]';
