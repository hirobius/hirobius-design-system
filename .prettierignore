# Build output
dist/
build/
.vercel/
coverage/
playwright-report/
test-results/

# Generated artifacts
DESIGN.md
# Generated token outputs — build-tokens.mjs owns their format (double-quoted
# JS/TS constants + raw CSS). Prettier's singleQuote reformat diverges from the
# generator output, which breaks check-token-rebake-needed on every token commit
# (it compares the raw build-tokens output to the staged files) and forced
# --no-verify. Let the generator own the format, same as exceptions-audit.md
# below. NB: the old `src/design-system/tokens/**` glob was a stale path and
# never matched these — that's what caused the churn.
src/app/design-system/generated-tokens.ts
src/app/design-system/generated-token-values.ts
src/app/design-system/generated-token-descriptions.ts
src/app/design-system/generated-token-vars.d.ts
src/app/design-system/generated-token-refs.ts
src/styles/tokens.css
src/styles/tokens.generated.css
src/app/data/**.json
public/hds-manifest.json
public/llms.txt
llms.txt
docs/guardrails/baselines/**
docs/guardrails/full-strictness-inventory.json
docs/ai/swarm-watchdog-decisions.jsonl
docs/ai/orchestration.json
# Generated audit report — written by the audit-exceptions gate every run (and
# the post-commit re-run). Prettier-reformatting it diverged from the generator
# output and churned the tree on every commit; let the generator own its format.
docs/audits/exceptions-audit.md

# Lock + node
pnpm-lock.yaml
node_modules/

# Worktree caches
.claude/worktrees/
.fixture-proof-cache.json

# Vendor
src/lib/vendor/**
