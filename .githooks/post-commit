#!/bin/sh
# HDS post-commit hook
# Updates the commit history feed for HdsHistoryCard (recent commits metadata).
# (Systems-log / journal machinery retired per ADR-010.)

set -eu

node scripts/update-commit-history.mjs
